# Sunshine Client v0.1.0-rc.4

纯命令行与系统服务改造的预发布版本。

- 移除托盘、Client 本地网页控制和登录自启入口；提供结构化 CLI、配对恢复、只读状态与诊断、配置修订比较、日志和显式服务管理。
- Windows SCM、Linux systemd、macOS launchd 管理长期运行；安装后默认停止，管理员明确启用开机运行。
- 维护与运行互斥，管理员先停服务再修改配置或凭据；保留身份与执行记录，不自动迁移历史格式。
- Windows x64、Linux x64、macOS Intel/Apple Silicon 的原生 CI 验证安装与服务生命周期；下载对应平台安装包/归档，并核对 SHA-256 与源码清单。

附件含 Windows MSI/ZIP、Linux DEB/tar.gz、两种 macOS tar.gz。协议固定为 Server 提交 `d4b98b06a00d185bd845a4bd3e3df8c939807864`；macOS Client 需要先部署能识别 macOS 平台的 [Server v0.10.1](https://github.com/isarmg/sunshine-manager-server/releases/tag/v0.10.1)。Sunshine 适配版本为 `2026.516.143833`。

Windows 已完成隔离真实 Sunshine 的只读认证与证书拒绝验证。Linux/macOS 真实 Sunshine 联调、真实受控配置写入与重启尚未完成。

安装产物未签名、未公证。本次未完成三平台物理重启后无人登录验收、全部真实网络故障与长时间运行矩阵，也不声明支持历史版本的自动升级或降级。Sunshine Client 安装器对现有状态/账户的覆盖拒绝仍有效；不得删除身份、队列或执行日志绕过检查。

CLI 用法与完整边界见 [改造说明](cli-unreleased.md) 和 [兼容矩阵](cli-compatibility.md)。发布不包含生产部署。
